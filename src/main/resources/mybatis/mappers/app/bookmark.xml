<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookmark">
	
	<!-- 즐겨찾기 추가 -->
	<insert id="add" parameterType="map">
		<![CDATA[
			insert into bookmark (`usersNo`, `itemNo`, `shopNo`, `itemCategoryNo`) VALUES(#{userNo }, #{itemNo }, shopno, itemcategoryno)
		]]>
	</insert>
	
	<!-- 즐겨찾기 삭제 -->
	<delete id="delete" parameterType="map">
		<![CDATA[
			delete from bookmark where itemNo = #{itemNo } and usersNo = #{userNo }
		]]>
	</delete>
	
	<!-- 즐겨찾기 검색 -->
	<select id="select" parameterType="map" resultType="long">
		<![CDATA[
			select no from bookmark where usersNo = #{userNo } and itemNo = #{itemNo }
		]]>
	</select>
</mapper>