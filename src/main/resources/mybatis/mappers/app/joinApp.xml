<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="join">
	<insert id="insertUser" parameterType="appuservo">
		<![CDATA[
			insert
			into users
			(id, password, gender, location, birth, `managerIdentified`, `shopNo`)
			values(#{id}, #{password}, #{gender}, #{location}, #{birth}, 1, null)
		]]>
	</insert>
	
	<insert id="insertOwner" parameterType="appuservo">
		<![CDATA[
			insert
			into users
			(id, password, gender, location, birth, `managerIdentified`, `shopNo`)
			values(#{id}, #{password}, #{gender}, #{location}, #{birth}, 2, null)
		]]>
	</insert>
</mapper>