<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="item2">

	<!-- 해당 매장의 상품 목록 -->
	<select id="shopItemList" parameterType="long" resultType="map">	
		<![CDATA[
			select i.*, s.name as shopName
			from item i left OUTER JOIN shop s ON s.no = i.`shopNo`
			where i.`shopNo` = #{shopNo }
		]]>
	</select>
	
	<!-- 상품 상세 정보 -->
	<select id="itemDetail" parameterType="long" resultType="itemvo">
		<![CDATA[
			select i.*, s.name, c.grade from (select avg(grade) as grade, shopNo
										from comments
									group by shopNo
									order by shopNo) c, item i left outer join shop s on i.`shopNo` = s.no where i.no = #{no }
		]]>
	</select>
	
</mapper>
